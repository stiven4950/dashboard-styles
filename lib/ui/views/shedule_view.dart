import 'package:flutter/material.dart';

class SheduleView extends StatelessWidget {
  const SheduleView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const SizedBox(
      child: Center(
        child: Text('Shedule View'),
      ),
    );
  }
}
